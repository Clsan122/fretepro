
export interface ClientFormData {
  name: string;     // Required field
  city: string;     // Required field
  state: string;    // Required field
  cnpj?: string;    // Optional field
  address?: string; // Optional field
  phone?: string;   // Optional field
}
