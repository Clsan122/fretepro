
import React from "react";
// Este componente não será mais utilizado, pois estamos usando o BottomNavigation
// Mantemos o arquivo vazio, mas ele pode ser excluído posteriormente
export const MobileNav: React.FC = () => {
  return null;
};

export default MobileNav;
